/** ==========================================================================
 * ALERT
 * ===========================================================================
 * alert(message);
 */
let msg = "Hello";
alert(msg);

/** ==========================================================================
 * PROMPT
 * ===========================================================================
 * result = prompt(title[, default]);
 * title: The text to show to the visitor.
 * default: An optional second parameter, the initial value for the input field.
 */
// let age = prompt('How old are you?', 26);
// alert(`You are ${age} years old!`);

// Non-Default
// let test = prompt('Test');
// For IE
// let test2 = prompt('Test2', '');

/** ==========================================================================
 * CONFIRM
 * ===========================================================================
 * result = confirm(question);
 * return true or false
 */
// let isMe = confirm("Are you me?");
// alert(isMe);
// If you choose the Ok button, The result will return TRUE.
// If you choose the Cancel button, The result will return FALSE.